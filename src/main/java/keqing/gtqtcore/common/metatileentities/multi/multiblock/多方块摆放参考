精确数值
.setExactLimit(1)
最大最小总数值
.setMaxGlobalLimited(1)
.setMinGlobalLimited(1)
预览显示数量
.setPreviewCount(1)
最大最小每层数值
.setMaxLayerLimited(1)
.setMinLayerLimited(1)
常用
.setExactLimit(1).setPreviewCount(1)
.setMinGlobalLimited(1).setMaxGlobalLimited(2).setPreviewCount(1)
输入输出仓
abilities(MultiblockAbility.IMPORT_FLUIDS)
abilities(MultiblockAbility.EXPORT_FLUIDS)
输入输出总线
abilities(MultiblockAbility.IMPORT_ITEMS)
abilities(MultiblockAbility.EXPORT_ITEMS)
能源仓 动力仓
abilities(MultiblockAbility.INPUT_ENERGY)
abilities(MultiblockAbility.EXPORT_ENERGY)
消声仓
abilities(MultiblockAbility.MUFFLER_HATCH)
维护仓
abilities(MultiblockAbility.MAINTENANCE_HATCH)

    public boolean hasMaintenanceMechanics() {
        return true;
    }

    public boolean hasMufflerMechanics() {
        return false;
    }

    public boolean canBeDistinct() {
        return true;
    }